---
id: DEC-070
title: "Change Record: chore: dogfood \u2014 A4 NFR vocabulary frozen + nfr-coverage\
  \ accounting parity (CHL-344)"
type: decision
status: approved
rationale: "chore: dogfood \u2014 A4 NFR vocabulary frozen + nfr-coverage accounting\
  \ parity (CHL-344). Changed: AUD-086, AUD-087, CHL-344, STORY-630."
tags:
- change-record
- auto-generated
suspect: false
links:
- target: AUD-086
  role: addresses
- target: AUD-087
  role: addresses
- target: CHL-344
  role: addresses
- target: STORY-630
  role: addresses
created: '2026-08-05'
review_status: unreviewed
risk_profile:
  tier: 2
  reversibility: irreversible
  blast_radius_count: 16
  confidence: ''
  confidence_reason: ''
dec_kind: change_record
fingerprint: sha256:f147a7cada60
modified: '2026-08-10'
---

# Change Record: chore: dogfood — A4 NFR vocabulary frozen + nfr-coverage accounting parity (CHL-344)

## Commit
- Hash: `1c429dc2d7097239de73765a7d5495e2a15e18f0`
- Author: longhuiberkeley <longhui@berkeley.edu>
- Date: 2026-08-05T16:21:14+08:00
- Subject: chore: dogfood — A4 NFR vocabulary frozen + nfr-coverage accounting parity (CHL-344)

### Message Body

CHL-344 sub-finding #3 (CODE half): the NFR surface exists but was never
exercised — 18/39 REQs carry no non_functional_category and every set
category is "functional"; the vocabulary had no source of truth (only CLI
help prose, already drifted from docs/cli-reference.md). And the accounting
anomaly: nfr-coverage was the ONLY REQ-quality warn that could drive exit-2
and block a release-gate project-audit (its siblings ac-coverage /
ac-observability are accounting after A2).

A4 (slice 5/6 of the CHL-344 workstream) fixes both without touching REQ
frontmatter (the data half — actually exercising categories beyond
`functional` — is A5, a separate human-approval slice):
- Vocabulary source of truth: NFR_CATEGORIES (8 values) + pure
  validate_nfr_category in lib/lint.py. `functional` stays SANCTIONED
  bookkeeping — the digit-gate exemption regression stays green.
- Create boundary: `create --nfr-category` now carries argparse choices
  generated from the constant; help prose can never drift again
  (docs/cli-reference.md aligned). `update --set
  non_functional_category=...` stays generic freeform on purpose — the lint
  warn below is its typo net.
- Typo net: new artifact-lint `nfr-category` check — one WARNING per REQ
  whose non-empty category is outside the vocabulary (missing field passes;
  never blocks/errors). Deliberately in artifact-lint, NOT lib/lint.py
  schema validation: artifact-lint warnings never feed project-audit's exit
  gate, whereas schema issues route through concern "consistency" and would
  escalate.
- Accounting parity: "nfr-coverage" registered in _ACCOUNTING_CONCERNS —
  the >=50%-missing warn still prints and stamps but can never drive exit-2.
  The nfr lens (extracted as pure _nfr_coverage_lens) gained one
  deterministic INFO line listing out-of-vocabulary values sorted (none
  live → no line).
- _CACHE_GENERATION bumped to 3: concern classification is part of the
  cached findings — a gen-2 cache would replay the nfr warn with its old
  escalating treatment and corrupt the stamp split.
- Tests: vocabulary matrix (all 8 values pass; unknown warns via the lint
  check, never blocks; missing passes); CLI choices rejection; lens INFO
  line determinism; run-level stamp test (nfr warn present → exit 0,
  summary_warns_escalating stays 0); functional exemption regression.
  +23 tests, 1183 green.

Deliberate omissions (fenced): no .specflow/schema/*.yaml enum key
(collides with the ROADMAP Schema-sync workstream's drift-lint-first
ordering); no NFR-specific test-link rules; no REQ frontmatter edits (A5).

Verified live: AUD-086 (fresh gen-3 recompute) and AUD-087 (cache-hit
replay) both stamp 0 errors / 25 warns (all accounting) / 57 infos, split
0/25, chain coverage 100 — stamps byte-identical to AUD-085, trend all-Δ0.
The nfr lens adds 0 warns live (18/39 missing is 46% < 50% → info; every
set category is `functional` → no out-of-vocabulary INFO line).

Tracking: CHL-344 Progress line + ROADMAP "Audit signal design" bullet
(A4 clause; NFR data exercising (A5) + workflow-tag rules remain). Exit
codes informational-only; --dry-run parity preserved (dry-run/full/cache-hit
all exit 0 with identical findings); _SUMMARY_STAMP_FIELDS and the A3
report section untouched; no version bump.

Gates: 1183 tests passed (+23); artifact-lint PASS (110 advisory warnings
vs 111 before — the new nfr-category check adds 0; the -1 is CHL-344's own
stale fingerprint refreshed after this slice's Progress-line edit);
project-audit --dry-run, full run, and cache-hit run all CLEAN with
identical exit 0 — 0 errors / 25 warns / 57 infos; AUD-086 minted on the
gen-3 fresh recompute, AUD-087 on the cache hit; both snapshots + the new
gen-3 cache committed; detect orphan-code: 0 orphans after retro-linking
tests/test_nfr_category.py to STORY-630 (224/224 files traced).

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>

## Changed Artifacts

| ID | Change Type |
|----|-------------|
| AUD-086 | content_modified |
| AUD-087 | content_modified |
| CHL-344 | content_modified |
| STORY-630 | content_modified |

## Impact Events

- `2026-08-05T08:10:43Z` — CHL-344 (content_modified, minor) flagged 0 downstream artifact(s)
- `2026-08-05T08:45:24Z` — CHL-344 (content_modified, minor) flagged 0 downstream artifact(s)
- `2026-08-05T09:19:01Z` — CHL-344 (content_modified, minor) flagged 0 downstream artifact(s)
