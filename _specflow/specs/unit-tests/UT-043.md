---
id: UT-043
title: Noise probe + concurrent-LOOP gate + rbac-check cleanup (small-fixes cluster)
type: unit-test
status: implemented
tags:
- backfilled
- verification-contract
- small-fixes
- noise-probe
- LOOP-gate
- REQ-035
suspect: false
links:
- target: STORY-SMALLFIX-621b
  role: verified_by
- target: REQ-035
  role: derives_from
created: '2026-08-04'
verify_command: uv run pytest tests/test_noise_probe.py tests/test_rbac_check.py tests/test_autoresearch_cli.py
  -q
fingerprint: sha256:961d751f2f10
verify_run_exit_code: 0
verify_run_out_hash: sha256:fddb85e3db79
verify_run_at: '2026-08-04T11:19:20Z'
verify_run_git_ref: 4d30162d8ab65c72ee7df75067253d4768e9b773
verify_run_command_hash: sha256:35b6674122ac
modified: '2026-08-04'
---

# Noise probe + concurrent-LOOP gate + rbac-check cleanup (small-fixes cluster)

Backfilled verification contract for STORY-SMALLFIX-621b (Small fixes - concurrent-LOOP gate, noise probe, test cleanup). Three coupled fixes:

- tests/test_noise_probe.py — exercises the noise probe: verifies metric variance stays below a configurable threshold before results are trusted (AC2).
- tests/test_rbac_check.py — exercises the rbac-check test cleanup: removes/stabilizes flaky or obsolete tests without reducing coverage (AC3).
- tests/test_autoresearch_cli.py — exercises the concurrent-LOOP gate (AC1): prevents starting a second LOOP on the same COMP while one is active.

Tagged backfilled: the fixes shipped in the v1.6.x/v1.13.x cluster; this contract is recorded now so the audit lens sees the verified_by link.

## Acceptance Criteria

1. uv run pytest tests/test_noise_probe.py tests/test_rbac_check.py tests/test_autoresearch_cli.py -q exits 0 (recorded via specflow verify).
2. Concurrent-LOOP gate refuses to start a second LOOP on the same COMP while one is active.
3. Noise probe verifies metric variance below a configurable threshold before trusting results.
4. Flaky/obsolete tests are removed or stabilized; coverage does not regress.
5. specflow artifact-lint --method programmatic adds 0 blocking issues.
