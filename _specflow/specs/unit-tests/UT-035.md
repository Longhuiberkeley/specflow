---
id: UT-035
title: Orphan-code detection + retro-link path (v1.13 adoption wave)
type: unit-test
status: implemented
rationale: 'Backfilled verification contract for STORY-624 (orphan-code adoption +
  outcome feedback loop). The detect orphan-code command and its retro-link path live
  in src/specflow/lib/orphans.py; tests/test_done.py exercises the detection + adoption
  closure path. Tagged backfilled: the detection code shipped with the v1.13.0 cycle,
  this contract is recorded now so the audit lens sees the verified_by link on the
  upcoming wave''s story.'
tags:
- backfilled
- verification-contract
- REQ-037
- v1.13
- orphan-code
suspect: false
links:
- target: STORY-624
  role: verified_by
- target: REQ-037
  role: derives_from
- target: ARCH-026
  role: derives_from
created: '2026-08-04'
verify_command: uv run pytest tests/test_done.py -q
fingerprint: sha256:d0c67e27b61a
verify_run_exit_code: 0
verify_run_out_hash: sha256:382f351a82f3
verify_run_at: '2026-08-04T11:10:53Z'
verify_run_git_ref: 4d30162d8ab65c72ee7df75067253d4768e9b773
verify_run_command_hash: sha256:847eb0c5d752
modified: '2026-08-04'
---

# Orphan-code detection + retro-link path (v1.13 adoption wave)

## Test Reference

`tests/test_done.py`

## Scope

Backfilled verification contract for STORY-624 ("Orphan-code adoption +
outcome feedback loop"). The detect orphan-code command and the retro-link
path live in `src/specflow/lib/orphans.py`; `tests/test_done.py` exercises
the detection + adoption closure path.

- `specflow detect orphan-code` reports un-adopted source clusters with no
  owning STORY/ARCH/DDD.
- The retro-link path lands an un-adopted cluster under an ARCH with a
  backfilled STORY in one step, tagged `backfilled`.
- The orphan meter coverage % rises after adoption (not just by demotion).

## Acceptance Criteria

1. `uv run pytest tests/test_done.py -q` exits 0 (recorded via
   `specflow verify`).
2. A one-step adopt-orphan path lands an un-adopted cluster under an ARCH with
   a backfilled STORY.
3. The orphan-code meter coverage % rises accordingly.
4. `specflow artifact-lint --method programmatic` adds 0 blocking issues.
