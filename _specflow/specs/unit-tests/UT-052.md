---
id: UT-052
title: 'rtm command: bidirectional requirements traceability matrix'
type: unit-test
status: implemented
tags:
- backfilled
- verification-contract
- REQ-DEFERRED-5cea
- v1.12.0
suspect: false
links:
- target: STORY-RTMCOMMA-1d6f
  role: verified_by
- target: REQ-DEFERRED-5cea
  role: derives_from
created: '2026-08-04'
verify_command: uv run pytest tests/test_rtm.py -q
fingerprint: sha256:f6c7f845837d
verify_run_exit_code: 0
verify_run_out_hash: sha256:207188a1c85f
verify_run_at: '2026-08-04T11:29:56Z'
verify_run_git_ref: 4d30162d8ab65c72ee7df75067253d4768e9b773
verify_run_command_hash: sha256:71c29b825460
modified: '2026-08-04'
---

# rtm command: bidirectional requirements traceability matrix

Backfilled verification contract for STORY-RTMCOMMA-1d6f (rtm command: bidirectional requirements traceability matrix, v1.12.0). The command lives in src/specflow/commands/rtm.py; tests/test_rtm.py exercises the matrix walk (REQ → ARCH → DDD → STORY → TEST, plus the upward inverse), the gap-row flagging for missing columns, the orphan-tests footer, the --format csv/markdown output, and the --req filter. No dedicated test file is named in the story's output_files, so the evidence is drawn from tests/test_rtm.py which exercises src/specflow/commands/rtm.py end-to-end via the real command path. Tagged backfilled: the work shipped in v1.12.0; this contract records the on-disk test evidence so the audit lens sees the verified_by link. Backfilled 2026-08-04.

## Acceptance Criteria

1. uv run pytest tests/test_rtm.py -q exits 0 (recorded via specflow verify).
2. specflow rtm renders a full-chain matrix (REQ, ARCH, DDD, STORY, TEST) for a healthy project.
3. Gap rows flag missing columns.
4. specflow rtm --format csv and --format markdown both emit parseable output.
5. specflow rtm --req <id> filters to one requirement.
6. specflow artifact-lint --method programmatic adds 0 blocking issues.
