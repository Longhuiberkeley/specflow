---
id: UT-033
title: 'v1.12.6 gate patch: project-audit dry-run + foundational-doctrine cry-wolf
  kill'
type: unit-test
status: implemented
rationale: 'Backfilled verification contract recording the test evidence that already
  supports STORY-622. The v1.12.6 gate patch (project-audit --dry-run, BP/DEC orphan-provenance
  exemption) is exercised by tests/test_project_audit.py (dry-run parity, write side-effect
  absence) and tests/test_artifact_lint.py (no regression in genuine orphan detection).
  Tagged backfilled: the test code shipped at v1.12.6, this contract is recorded now
  so the audit lens sees the verified_by link.'
tags:
- backfilled
- verification-contract
- REQ-037
- v1.12.6
suspect: false
links:
- target: STORY-622
  role: verified_by
- target: REQ-037
  role: derives_from
- target: ARCH-026
  role: derives_from
created: '2026-08-04'
verify_command: uv run pytest tests/test_project_audit.py tests/test_artifact_lint.py
  -q
fingerprint: sha256:626de76e0b2d
verify_run_exit_code: 0
verify_run_out_hash: sha256:18359a770f49
verify_run_at: '2026-08-04T11:10:49Z'
verify_run_git_ref: 4d30162d8ab65c72ee7df75067253d4768e9b773
verify_run_command_hash: sha256:9cf4dd40345c
modified: '2026-08-04'
---

# v1.12.6 gate patch: project-audit dry-run + foundational-doctrine cry-wolf kill

## Test Reference

`tests/test_project_audit.py` and `tests/test_artifact_lint.py`

## Scope

Backfilled verification contract for STORY-622 ("v1.12.6 gate patch: kill the
foundational-doctrine cry-wolf + add project-audit --dry-run"). The work shipped
in v1.12.6; this artifact records the test evidence that already exists on disk
so the audit lens can walk a verified_by link from the STORY.

- `tests/test_project_audit.py` — exercises `specflow project-audit --dry-run`:
  same findings and exit code as a real run, no AUD/CHL artifacts written, no
  cache/index mutations, no audit snapshot dir created.
- `tests/test_artifact_lint.py` — confirms the has_provenance exemption for
  best-practice and decision types does not regress genuine orphan detection on
  other types.

## Acceptance Criteria

1. `uv run pytest tests/test_project_audit.py tests/test_artifact_lint.py -q`
   exits 0 (recorded via `specflow verify`).
2. A BP or DEC with no `links[]` is NOT flagged as orphan-provenance.
3. Genuine orphan-provenance detection for every other type stays intact.
4. `specflow project-audit --dry-run` exits with the same code as a real run
   and writes nothing to disk.
