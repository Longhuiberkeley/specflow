---
id: IT-026
title: 'Integration Test for ARCH-023: Four-tier research artifact hierarchy'
type: integration-test
status: verified
rationale: Backfilled integration test for ARCH-023 (Four-tier research artifact hierarchy).
  The hierarchy (COMP/LOOP/EXPT/FIND) and link roles (operates_on, belongs_to, condenses)
  are exercised by tests/test_trace_research.py (locks the link-role contract) and
  tests/test_autoresearch_pack.py (end-to-end chain walks the hierarchy with the correct
  roles).
tags:
- backfilled
- verification-contract
- autoresearch
- hierarchy
- ARCH-023
suspect: false
links:
- target: ARCH-023
  role: verified_by
- target: STORY-067
  role: verified_by
- target: STORY-069
  role: verified_by
created: '2026-08-04'
fingerprint: sha256:e1a6f09e6b62
verify_command: uv run pytest tests/test_trace_research.py tests/test_autoresearch_pack.py
  tests/test_rtm.py -q
modified: '2026-08-04'
verify_run_exit_code: 0
verify_run_out_hash: sha256:ee2d58c34a13
verify_run_at: '2026-08-04T11:42:45Z'
verify_run_git_ref: 4d30162d8ab65c72ee7df75067253d4768e9b773
verify_run_command_hash: sha256:45e6ee193e4c
---

# Integration Test for ARCH-023: Four-tier research artifact hierarchy

Backfilled integration test for ARCH-023 (Four-tier research artifact hierarchy).

## Test Reference

- tests/test_trace_research.py — exercises the link-role contract from ARCH-023 §40: LOOP--[operates_on]-->COMP, EXPT--[belongs_to]-->LOOP, FIND--[belongs_to]-->COMP, FIND--[condenses]-->LOOP. These tests trap the regression where the renderers used derives_from for everything.
- tests/test_autoresearch_pack.py — exercises the end-to-end chain (COMP→LOOP→3 EXPTs→FIND) and confirms trace COMP walks the hierarchy with the documented roles.
- tests/test_rtm.py — exercises the bidirectional REQ→ARCH→STORY→tests walk that the research chain participates in.

## Scope

ARCH-023 defines the four-tier hierarchy (COMP>LOOP>EXPT) plus the FIND tier that condenses knowledge. The link roles are the source of truth for queries like 'all EXPTs for this COMP' or 'what FINDs feed the next LOOP'. The tests lock in:
- the four roles
- the absence of derives_from for research-internal links
- the chain walk via specflow trace COMP

## Acceptance Criteria

1. uv run pytest tests/test_trace_research.py tests/test_autoresearch_pack.py tests/test_rtm.py -q exits 0 (recorded via specflow verify).
2. The four link roles (operates_on, belongs_to, condenses) are present in the rendered chain.
3. specflow artifact-lint --method programmatic adds 0 blocking issues.
