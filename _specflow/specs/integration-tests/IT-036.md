---
id: IT-036
title: 'Integration Test for STORY-630: project-audit --baseline anchor and drift
  wiring'
type: integration-test
status: verified
tags:
- verification-contract
- baselines
- semver
- REQ-035
suspect: false
links:
- target: STORY-630
  role: verified_by
- target: REQ-035
  role: derives_from
created: '2026-08-05'
verify_command: uv run pytest tests/test_project_audit.py tests/test_evidence.py -q
fingerprint: sha256:d8344e84998e
modified: '2026-08-05'
verify_run_exit_code: 0
verify_run_out_hash: sha256:6054e89d2a36
verify_run_at: '2026-08-05T05:10:30Z'
verify_run_git_ref: 8640436303e6bf814b80f4573d4a57f9a0284f86
verify_run_command_hash: sha256:72cb766c8241
---

# Integration Test for STORY-630: project-audit --baseline anchor and drift wiring

## Test Reference

- `tests/test_project_audit.py::TestDriftSelectionAndBaselineAnchor::test_mixed_names_drift_compares_releases_not_freeform` — with [v1.0.0, v1.1.0, snapshot] on disk the audit diffs v1.0.0 → v1.1.0, not the raw tail.
- `tests/test_project_audit.py::TestDriftSelectionAndBaselineAnchor::test_pure_freeform_falls_back_to_raw_tail` — pure-freeform histories keep the raw-tail pair.
- `tests/test_project_audit.py::TestDriftSelectionAndBaselineAnchor::test_known_baseline_anchors_drift` — `--baseline v1.0.0` widens drift to v1.0.0 → newest release; the anchored run neither reads nor poisons the findings cache.
- `tests/test_project_audit.py::TestDriftSelectionAndBaselineAnchor::test_unknown_baseline_warns_and_falls_back` — unknown name warns loudly and keeps the auto-pair outcome.
- `tests/test_evidence.py::TestPredecessorSelection` — the evidence report's predecessor follows the same policy (release target, freeform target vs newest release, pure-freeform fallback).

## Scope

Exercises the wired path end to end through `project_audit.run()` and `evidence.generate_evidence_report()`: selection policy in the real cross-cutting pipeline, the --baseline anchor with warn-and-fallback, and cache safety.

## Acceptance Criteria

1. The exact verify command exits 0.
2. A known --baseline name anchors drift; an unknown name warns + falls back with the auto-pair exit code.
3. --dry-run writes nothing and matches the writing run's exit code on both anchor paths.
