---
id: IT-031
title: 'Integration Test for ARCH-028: Deferred lifecycle capabilities command surfaces'
type: integration-test
status: verified
rationale: "Backfilled integration test for ARCH-028 (Deferred lifecycle capabilities\
  \ \u2014 accounting-based command surfaces). The six accounting-based command surfaces\
  \ (phase-set, rtm, rbac check, supersession, AC/NFR gates, multi-host) are exercised\
  \ by tests/test_phase_set.py, tests/test_rtm.py, tests/test_rbac_check.py, tests/test_supersession.py,\
  \ tests/test_lint_quality_gates.py, and tests/test_multi_platform.py."
tags:
- backfilled
- verification-contract
- ARCH-028
- lifecycle
- v1.12
- accounting-not-policing
suspect: false
links:
- target: ARCH-028
  role: verified_by
- target: STORY-PHASESET-d470
  role: verified_by
- target: STORY-RTMCOMMA-1d6f
  role: verified_by
- target: STORY-RBACCHEC-e356
  role: verified_by
- target: STORY-SUPERSES-c408
  role: verified_by
- target: STORY-ARTIFACT-a39a
  role: verified_by
- target: STORY-MULTIHOS-63a5
  role: verified_by
created: '2026-08-04'
fingerprint: sha256:f19a1e5ea020
verify_command: uv run pytest tests/test_phase_set.py tests/test_rtm.py tests/test_rbac_check.py
  tests/test_supersession.py tests/test_lint_quality_gates.py tests/test_multi_platform.py
  -q
modified: '2026-08-04'
verify_run_exit_code: 0
verify_run_out_hash: sha256:05c8bdd84fa8
verify_run_at: '2026-08-04T11:44:59Z'
verify_run_git_ref: 4d30162d8ab65c72ee7df75067253d4768e9b773
verify_run_command_hash: sha256:a97e260408a6
---

# Integration Test for ARCH-028: Deferred lifecycle capabilities command surfaces

Backfilled integration test for ARCH-028 (Deferred lifecycle capabilities — accounting-based command surfaces).

## Test Reference

- tests/test_phase_set.py — exercises specflow phase-set's forward/rewind transition history; the six lifecycle stories (STORY-PHASESET-d470, etc.) all closed against this surface.
- tests/test_rtm.py — exercises specflow rtm's bidirectional REQ→ARCH→STORY→tests walk with --gaps filter and the orphan-test footer.
- tests/test_rbac_check.py — exercises specflow rbac check's author→roles resolution and transition authorization; single-user projects get a clear not-active message with exit 0.
- tests/test_supersession.py — exercises the supersession status and supersedes role for REQ/ARCH/DDD schemas.
- tests/test_lint_quality_gates.py — exercises the AC/NFR lint parsers (empty-AC error, NFR threshold warning).
- tests/test_multi_platform.py — exercises the multi-host platform detection warning and refresh --all-platforms.
- tests/test_hook.py — exercises the lifecycle hook that wires the six surfaces into the pre-commit / pre-push flow.

## Scope

ARCH-028 unifies the six deferred capabilities by being accounting/recording surfaces over the existing artifact graph. None becomes a gate. The integration tests confirm:
- each surface exits 0 on the success path
- each surface surfaces gaps as warnings
- each surface participates in the lifecycle hook flow

## Acceptance Criteria

1. uv run pytest tests/test_phase_set.py tests/test_rtm.py tests/test_rbac_check.py tests/test_supersession.py tests/test_lint_quality_gates.py tests/test_multi_platform.py -q exits 0 (recorded via specflow verify).
2. Each of the six commands surfaces gaps as warnings, not errors.
3. specflow artifact-lint --method programmatic adds 0 blocking issues.
