---
id: IT-032
title: 'Integration Test for ARCH-LOUDFAIL: Loud-failure and advisory ergonomics'
type: integration-test
status: verified
rationale: Backfilled integration test for ARCH-LOUDFAIL-d87a (Loud-failure and advisory
  ergonomics command surfaces). The write-conflict, mutation, repair-hint, and graph-accounting
  surfaces are exercised by tests/test_v132_ergonomics.py (loud-fail + advisory),
  tests/test_brief.py (advisory surfacing), and tests/test_hook.py (write-conflict
  detection in the install/refresh hook).
tags:
- backfilled
- verification-contract
- v1.13.2
- ARCH-LOUDFAIL
- ergonomics
suspect: false
links:
- target: ARCH-LOUDFAIL-d87a
  role: verified_by
- target: STORY-FIXACCEP-f941
  role: verified_by
- target: STORY-SHIPV1-cbb2
  role: verified_by
created: '2026-08-04'
fingerprint: sha256:ec70eb08d617
verify_command: uv run pytest tests/test_v132_ergonomics.py tests/test_brief.py tests/test_hook.py
  -q
modified: '2026-08-04'
verify_run_exit_code: 0
verify_run_out_hash: sha256:cbbc63892ec8
verify_run_at: '2026-08-04T11:45:26Z'
verify_run_git_ref: 4d30162d8ab65c72ee7df75067253d4768e9b773
verify_run_command_hash: sha256:8ddb13fc48d3
---

# Integration Test for ARCH-LOUDFAIL: Loud-failure and advisory ergonomics

Backfilled integration test for ARCH-LOUDFAIL-d87a (Loud-failure and advisory ergonomics command surfaces).

## Test Reference

- tests/test_v132_ergonomics.py — exercises the loud-failure surface: precise body mutation (no false-positive diffs on whitespace), validated nested merges (refuse partial writes), actionable repair hints (recommend the exact field path to fix), one-pass graph accounting (no double-counting across types).
- tests/test_brief.py — exercises brief --next's advisory surfacing of the loud-fail findings (Tip blocks, never blocking).
- tests/test_hook.py — exercises the install/refresh hook's write-conflict detection and the repair-hint it produces when a user-edited file is in the way.
- tests/test_autoresearch_schema.py — touches the body-mutation boundary on acceptance-criteria sections (no-space sibling headings, ignore fenced headings during boundary selection — STORY-FIXACCEP-f941).
- tests/test_risk.py — exercises the read-only risk-tier path that consumes the same artifact graph and stays advisory.
- tests/test_baselines.py — exercises the baseline management path that hooks into the advisory surface.

## Scope

ARCH-LOUDFAIL-d87a centralizes authoring semantics in Python helpers and commands:
- precise body mutation (no whitespace false-positives)
- validated nested merges (refuse partial writes)
- actionable repair hints (recommend field path)
- one-pass graph accounting (no double-count)

CLI and skills remain thin wrappers around the Python helpers. Advisory-only — no new gates.

## Acceptance Criteria

1. uv run pytest tests/test_v132_ergonomics.py tests/test_brief.py tests/test_hook.py -q exits 0 (recorded via specflow verify).
2. Loud-fail surfaces never produce blocking exit codes.
3. specflow artifact-lint --method programmatic adds 0 blocking issues.
