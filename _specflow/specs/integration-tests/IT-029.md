---
id: IT-029
title: 'Integration Test for ARCH-026: Verification-contract recording + accounting
  evidence lenses'
type: integration-test
status: verified
rationale: Backfilled integration test for ARCH-026 (Verification-contract recording
  + accounting evidence lenses). The specflow verify command + verify_run_* recording
  + the two accounting lenses (missing-evidence, divergence) are exercised end-to-end
  by tests/test_verify.py (recorder + evidence pipeline), tests/test_project_audit.py
  (--dry-run parity, accounting lens warnings), and tests/test_artifact_lint.py (no
  regression in genuine orphan detection).
tags:
- backfilled
- verification-contract
- v1.13
- ARCH-026
- accounting-not-policing
suspect: false
links:
- target: ARCH-026
  role: verified_by
- target: STORY-622
  role: verified_by
- target: STORY-623
  role: verified_by
- target: STORY-624
  role: verified_by
- target: STORY-626
  role: verified_by
created: '2026-08-04'
fingerprint: sha256:d5c8c5525269
verify_command: uv run pytest tests/test_verify.py tests/test_project_audit.py tests/test_artifact_lint.py
  -q
modified: '2026-08-04'
verify_run_exit_code: 0
verify_run_out_hash: sha256:294d7672b7eb
verify_run_at: '2026-08-04T11:44:12Z'
verify_run_git_ref: 4d30162d8ab65c72ee7df75067253d4768e9b773
verify_run_command_hash: sha256:dcdaf7411f17
---

# Integration Test for ARCH-026: Verification-contract recording + accounting evidence lenses

Backfilled integration test for ARCH-026 (Verification-contract recording + accounting evidence lenses).

## Test Reference

- tests/test_verify.py — exercises the specflow verify command end-to-end: reads verify_command from frontmatter, runs it, records verify_run_exit_code + verify_run_out_hash + verify_run_at + verify_run_git_ref + verify_run_command_hash, and surfaces a Tip on divergence.
- tests/test_project_audit.py — exercises project-audit --dry-run parity with a real run, plus the two accounting lenses (missing-evidence, divergence) surfacing warnings without driving the gate exit code.
- tests/test_artifact_lint.py — exercises the has_provenance exemption for BP/DEC (cry-wolf fix from v1.12.6) and confirms genuine orphan detection is intact.
- tests/test_brief.py — exercises brief --next's advisory surfacing of the accounting findings.
- tests/test_risk.py — exercises the read-only risk-tier path that consumes recorded verify_run_exit_code.

## Scope

ARCH-026 defines the two coupled mechanisms: (1) specflow verify records real run evidence; (2) accounting lenses surface evidence gaps as warnings. No new artifact types, no new link roles (D-18 respected), zero external API calls. The integration tests confirm the recorder, the lenses, and the brief advisory all work together without ever becoming a gate.

## Acceptance Criteria

1. uv run pytest tests/test_verify.py tests/test_project_audit.py tests/test_artifact_lint.py -q exits 0 (recorded via specflow verify).
2. specflow verify records verify_run_* fields deterministically.
3. specflow project-audit --dry-run writes nothing to disk and exits with the same code as a real run.
4. specflow artifact-lint --method programmatic adds 0 blocking issues.
