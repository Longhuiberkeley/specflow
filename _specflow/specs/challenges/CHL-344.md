---
id: CHL-344
title: 'Audit signal design: severity asymmetry, no audit-over-audit trend, unclassified
  ACs at info'
type: challenge
status: open
suspect: false
links:
- target: AUD-058
  role: refers_to
created: '2026-08-04'
severity: info
technique: assumption_surfacing
fingerprint: sha256:d8c9424c87fc
modified: '2026-08-05'
version: 2
---

# Audit signal design: severity asymmetry, no audit-over-audit trend, unclassified ACs at info

Consolidated audit signal-design findings from the AUD-058 adversarial wings (premortem, assumption_surfacing, regulator, temporal_drift):
- Severity asymmetry: the same problem class (REQ with ACs but zero linked tests) is warn for 8 REQs and info for ~20 others; info-classed rows mirror warn-classed CHL-338/339 debt, so triaging the warn layer leaves the debt to regenerate.
- No trend vs prior audit: nothing reports whether the 79 verification gaps are growing or shrinking between audits.
- 157 of 243 AC items are observability-unclassified (effectively unverifiable) at info severity; per-REQ rollup is too coarse to act on.
- 18/39 REQs carry no non_functional_category and every set category is "functional" — the NFR surface exists but is never exercised.
- Workflow tags (backfilled, deferred) assert no structural rule; anything tagged backfilled currently bypasses test-link expectations.

Recommend folding these into the ROADMAP Future list as a scoped "audit signal design" workstream rather than holding them as open challenges.

## Progress

2026-08-05: sub-finding #2 (no trend vs prior audit) is RESOLVED — audit report headers now render trend deltas vs the prior stamped AUD, and new AUD artifacts stamp machine-readable summary counts + chain_coverage_pct (see CHL-341's Resolution Evidence). Remaining sub-findings (severity asymmetry, per-AC rows, NFR exercising, workflow-tag structural rules) stay in the ROADMAP Future "Audit signal design" workstream; this CHL remains its umbrella tracker.

2026-08-05: A0 lossless findings cache + generation key shipped (slice 1/6 of the remaining workstream). Before: a cached audit replayed only 20 findings (the `all_findings_raw[:20]` cap) while AUD-075's stamp said summary_info=187, and the fingerprint keyed on artifact content only so lens-code changes could not invalidate replays. After: the cache is lossless (full-set round-trip regression-tested) and carries a bumpable `_CACHE_GENERATION` key that invalidates stale replays repo-wide.

2026-08-05: A1 escalating/accounting stamp split shipped (slice 2/6) — trend-safe prerequisite for A2 severity unification. New AUD artifacts stamp `summary_warns_escalating` / `summary_warns_accounting` alongside (never inside) the baseline summary stamp, and the trend line extends the warns bullet with the split only when both the current run and the prior AUD carry it — pre-A1 priors (AUD-073..080) keep rendering legacy totals, never the "predates summary stamping" fallback. So when A2 moves ~25 ac-coverage findings from info to warn, the honest trend reads "escalating 0→0, accounting 0→25" instead of a fake "+25 warns" regression. Informational only — findings, exit codes, `--dry-run` parity unchanged.

2026-08-05: A2 severity unification shipped (slice 3/6) — ac-coverage is now ONE problem class at ONE severity: both degrees (zero linked tests; fewer-than-AC-count linked tests) emit WARN, with the degree kept in the message text ("(N green) — review coverage"). ac-coverage stays registered in `_ACCOUNTING_CONCERNS` (accounting parity), so those warns print + stamp but never drive exit-2; `_CACHE_GENERATION` bumped to 2 because severity is part of the cached findings. First fresh audit (AUD-082) trend reads "warns 0→25 (Δ+25) (escalating 0→0, accounting 0→25)" — escalating flat, accounting carries all the movement. Read-as-debt-tracker note: no NEW grouped CHL minted — title-based dedup matched CHL-338, which already owns the stable grouped title "Audit cross-cutting/ac-coverage findings" (status: addressed). CHL-338 therefore reads as the existing umbrella debt tracker for the resurfaced warn-classed ac-coverage debt, NOT as a regression; the status-blind dedup hole (resurfaced debt whose CHL is addressed gets no fresh tracker) is noted in the ROADMAP workstream.

2026-08-05: A3 shipped (slice 4/6) — per-AC observability rows in report body + full table in subagent-cross-cutting.md, findings contract untouched. `classify_reqs_observability` now carries its per-AC `items` out (one additive key; `classify_ac_observability` remains THE single AC-parse path — nothing re-parses AC text), and the audit report body gains an `## AC observability detail` appendix: 160 unclassified/aspirational rows surfaced of 246 total AC items across 39 REQs (157 unclassified + 3 aspirational; observable rows omitted as the noise bound). Judge-verified placement honored: the rows are REPORT BODY, not findings — AUD-084's findings/stamp are byte-identical to AUD-083 (0 errors / 25 warns all accounting / 57 infos; split 0/25; chain coverage 100%), no `_CACHE_GENERATION` bump (the cache stores findings, not report sections). The full all-class 246-row table lives in subagent-cross-cutting.md. Determinism contract: sorted REQ IDs + document-order items, section byte-identical across fresh run, cache hit, and --dry-run; suppressed under --quick with the cross-cutting analysis it accompanies.

2026-08-05: A4 shipped (slice 5/6) — vocabulary frozen in code, create-boundary choices, artifact-lint typo net, nfr-coverage accounting parity. Sub-finding #3 CODE half (the data half — actually exercising categories beyond `functional` in REQ frontmatter — is A5, a separate human-approval slice; A4 edits zero REQ frontmatter). (1) Vocabulary source of truth: `NFR_CATEGORIES` (8 values; `functional` stays SANCTIONED bookkeeping — the digit-gate exemption regression stays green) + pure `validate_nfr_category` in lib/lint.py; the create boundary's `--nfr-category` now carries `choices` generated from the constant (help prose can no longer drift; docs/cli-reference.md aligned), while `update --set non_functional_category=...` stays generic freeform on purpose. (2) Typo net: new artifact-lint `nfr-category` check — one WARNING per REQ whose non-empty category is outside the vocabulary (missing field passes; never blocks/errors; adds 0 warns live since every set category is `functional`). It lives in artifact-lint, NOT lib/lint.py schema validation, so it can never route through concern "consistency" into the exit gate. (3) Accounting parity: `nfr-coverage` registered in `_ACCOUNTING_CONCERNS` — pre-A4 it was the ONLY REQ-quality warn that could drive exit-2 and block a release-gate project-audit; the >=50%-missing warn still prints and stamps, just never escalates. The nfr lens (extracted as `_nfr_coverage_lens`) gained one deterministic INFO line listing out-of-vocabulary values sorted (none live → no line); `_CACHE_GENERATION` bumped to 3 (concern classification is part of cached findings). Gates: 1183 tests (+23); artifact-lint PASS 111 warns before == after; dry-run/full/cache-hit audits all CLEAN exit 0 with identical 0 errors / 25 warns (all accounting) / 57 infos — AUD-086 minted on the gen-3 fresh recompute, AUD-087 on the cache hit, both stamps byte-identical to AUD-085 (escalating 0→0, accounting 25→25, chain coverage 100% unchanged). Deliberate omissions fenced: no `.specflow/schema/*.yaml` enum key (collides with the ROADMAP Schema-sync workstream's drift-lint-first ordering), no NFR-specific test-link rules, no REQ frontmatter edits (A5).

2026-08-05: A5 NFR dogfood sweep shipped (slice 6/6 code+data; user-approved per-artifact) — all 18 REQs omitting `non_functional_category` walked against the CKL-REV-REQ-02 semantic bar; every one is genuinely a feature/capability requirement, so all 18 backfilled `functional` via `specflow update --set`. Recorded outcome: ZERO genuine NFRs in this project — an explicitly accepted result; the NFR surface is now honestly exercised (histogram 0/39 missing; `functional(39)`; fresh audit 56 infos, the missing-category line gone). Mechanical stamping of fancier categories was prohibited. Sweep discovery (recorded for follow-up, not fixed in this slice): artifact fingerprints hash the body only (`compute_fingerprint(body)`), so frontmatter-only edits do not invalidate the audit findings cache — the sweep's own category writes were invisible to a cache-hit audit until the cache was purged; same exposure applies to frontmatter-only link/status edits. Candidate fix: fold a frontmatter component into `_project_fingerprint`.
