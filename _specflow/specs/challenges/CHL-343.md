---
id: CHL-343
title: project-audit baseline drift compares v1.9.0->v1.9.2 despite newer baselines
  through v1.13.3
type: challenge
status: addressed
suspect: false
links:
- target: AUD-058
  role: refers_to
- target: STORY-627
  role: refers_to
- target: UT-045
  role: refers_to
- target: AUD-063
  role: refers_to
- target: CHL-344
  role: refers_to
created: '2026-08-04'
severity: warn
technique: regulator
fingerprint: sha256:3c1534c44781
modified: '2026-08-04'
version: 2
---

# project-audit baseline drift compares v1.9.0->v1.9.2 despite newer baselines through v1.13.3

AUD-058 report header states "Baseline drift: compared v1.9.0 -> v1.9.2", but .specflow/baselines/ contains baselines through v1.13.3. The baseline-selection logic picks a stale pair, so suspect-flag replay for artifacts modified in v1.12.x-v1.13.x releases runs against a baseline four minor versions behind.

Lens: regulator.

Recommended actions:
1. Fix baseline selection to use the most recent baseline by default.
2. Emit a warn when the comparison baseline is older than the most recent shipped baseline.

## Resolution Evidence

The reported defect is resolved by semver-aware baseline ordering in STORY-627, including numeric-segment and prerelease regression coverage in UT-045. AUD-063 confirms project-audit compares v1.13.2 to v1.13.3. Recommended action 2 (a separate stale-comparison warning) is not implemented; it is deferred under the ROADMAP audit-signal-design workstream tracked by CHL-344 because corrected default selection now always chooses the two newest baselines.
