---
id: QT-002
title: ReqIF Round-Trip Quality
type: qualification-test
status: implemented
rationale: Qualification tests verifying ReqIF import/export with deterministic UUIDs
  and metadata preservation
suspect: false
links:
- target: REQ-011
  role: verified_by
- target: STORY-044
  role: verified_by
created: '2026-04-21'
fingerprint: sha256:539792f23420
version: 2
---

# ReqIF Round-Trip Quality

## Test Reference

`tests/test_reqif_roundtrip.py`

## Test Cases

### TC-001: Round-Trip Field Preservation
- **Input**: Create REQ artifact, export to ReqIF, re-import
- **Expected**: id, title, status, body preserved exactly
- **Verification**: Re-imported artifact matches original

### TC-002: Metadata Preservation
- **Input**: REQ with `reqif_metadata` field, export, re-import
- **Expected**: `reqif_metadata` survives round-trip
- **Verification**: Frontmatter contains original metadata dict

### TC-003: Deterministic UUIDs
- **Input**: Export same artifact twice
- **Expected**: Identical UUIDs in both exports
- **Verification**: `uuid5(NAMESPACE_OID, id + hash)` produces same value

### TC-004: Import Correctness
- **Input**: Valid ReqIF archive with 3 REQ objects
- **Expected**: 3 REQ artifacts created with correct frontmatter
- **Verification**: Files exist, IDs sequential, links valid

## Acceptance Criteria

1. All test cases in `tests/test_reqif_roundtrip.py` pass
2. Round-trip preserves all core fields
3. Deterministic UUIDs enable idempotent exchanges
