---
id: QT-032
title: 'Qualification Test for REQ-031: Explore / exploit / validate LOOP modes'
type: qualification-test
status: verified
rationale: Backfilled qualification test for REQ-031 (Explore / exploit / validate
  LOOP modes). Each LOOP declares one of three modes that influences the agent's ideation
  behavior in Phase 2 of the loop protocol. Exercised by tests/test_autoresearch_cli.py
  (TestPlanCreateUpdate confirms the mode field is set, stored, and round-tripped).
  Stories STORY-070 (mode field on LOOP schema) and STORY-072 (protocol doc) close
  against this REQ.
tags:
- backfilled
- verification-contract
- autoresearch
- modes
- REQ-031
suspect: false
links:
- target: REQ-031
  role: verified_by
- target: STORY-070
  role: verified_by
- target: STORY-072
  role: verified_by
created: '2026-08-04'
fingerprint: sha256:b21c7cb533bf
verify_command: uv run pytest tests/test_autoresearch_cli.py tests/test_autoresearch_pack.py
  -q
modified: '2026-08-04'
verify_run_exit_code: 0
verify_run_out_hash: sha256:7569fc7821ad
verify_run_at: '2026-08-04T11:47:00Z'
verify_run_git_ref: 4d30162d8ab65c72ee7df75067253d4768e9b773
verify_run_command_hash: sha256:ac829fa0debb
---

# Qualification Test for REQ-031: Explore / exploit / validate LOOP modes

Backfilled qualification test for REQ-031 (Explore / exploit / validate LOOP modes).

## Test Reference

- tests/test_autoresearch_cli.py — exercises the plan verb's mode field: TestPlanCreateUpdate confirms specflow autoresearch plan creates a LOOP with the explore/exploit/validate mode, and that the field round-trips through update.
- tests/test_autoresearch_pack.py — exercises the LOOP schema's status/mode lifecycle (TestSchemaLifecycle) which includes the mode field as a required enum.

## Scope

REQ-031 requires each LOOP to declare one of three modes (explore / exploit / validate) that influences the agent's ideation behavior in Phase 2 of the loop protocol. Different research phases need different ideation strategies: first-loop exploration is creative; post-discovery exploitation is refinement; validation is re-running best approaches on different data.

## Acceptance Criteria

1. uv run pytest tests/test_autoresearch_cli.py tests/test_autoresearch_pack.py -q exits 0 (recorded via specflow verify).
2. specflow autoresearch plan accepts a --mode flag (explore/exploit/validate) and persists it.
3. specflow artifact-lint --method programmatic adds 0 blocking issues.
