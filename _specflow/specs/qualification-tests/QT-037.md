---
id: QT-037
title: 'Qualification Test for REQ-037: Eliminate false-confidence signals (v1.13
  cycle)'
type: qualification-test
status: verified
rationale: 'Backfilled qualification test for REQ-037 (Eliminate false-confidence
  signals). The v1.13 cycle closes two gaps: (1) a verification-contract mechanism
  that records real run evidence (specflow verify + verify_run_* fields), and (2)
  accounting lenses that surface gaps as warnings without driving the gate exit code.
  Exercised by tests/test_verify.py (the recorder), tests/test_project_audit.py (--dry-run
  parity, lens warnings), tests/test_brief.py (advisory surfacing), tests/test_artifact_lint.py
  (cry-wolf kill for BP/DEC). Stories STORY-622/623/626 close against this REQ.'
tags:
- backfilled
- verification-contract
- v1.13
- REQ-037
- accounting-not-policing
suspect: false
links:
- target: REQ-037
  role: verified_by
- target: STORY-622
  role: verified_by
- target: STORY-623
  role: verified_by
- target: STORY-626
  role: verified_by
created: '2026-08-04'
fingerprint: sha256:c348fc9593c8
verify_command: uv run pytest tests/test_verify.py tests/test_project_audit.py tests/test_artifact_lint.py
  tests/test_brief.py -q
modified: '2026-08-04'
verify_run_exit_code: 0
verify_run_out_hash: sha256:236e6432261f
verify_run_at: '2026-08-04T11:48:50Z'
verify_run_git_ref: 4d30162d8ab65c72ee7df75067253d4768e9b773
verify_run_command_hash: sha256:cfe59fe6f310
---

# Qualification Test for REQ-037: Eliminate false-confidence signals (v1.13 cycle)

Backfilled qualification test for REQ-037 (Eliminate false-confidence signals — v1.13 cycle).

## Test Reference

- tests/test_verify.py — exercises specflow verify end-to-end: reads verify_command from frontmatter, runs it, records verify_run_exit_code + verify_run_out_hash + verify_run_at + verify_run_git_ref + verify_run_command_hash, and surfaces a Tip on divergence.
- tests/test_project_audit.py — exercises project-audit --dry-run parity with a real run, plus the two accounting lenses (missing-evidence, divergence) surfacing warnings without driving the gate exit code.
- tests/test_artifact_lint.py — exercises the has_provenance exemption for BP/DEC (cry-wolf fix from v1.12.6) and confirms genuine orphan detection is intact.
- tests/test_brief.py — exercises brief --next's advisory surfacing of the accounting findings.
- tests/test_risk.py — exercises the read-only risk-tier path that consumes recorded verify_run_exit_code.

## Scope

REQ-037 requires two coupled mechanisms: (1) a verification-contract recorder (specflow verify) that turns the verified status from an assertion into recorded, machine-checkable evidence; (2) accounting lenses that surface evidence gaps as warnings without driving the gate. Throughout, accounting never becomes policing.

## Acceptance Criteria

1. uv run pytest tests/test_verify.py tests/test_project_audit.py tests/test_artifact_lint.py tests/test_brief.py -q exits 0 (recorded via specflow verify).
2. specflow verify records verify_run_* fields deterministically.
3. specflow project-audit --dry-run writes nothing to disk and exits with the same code as a real run.
4. specflow artifact-lint --method programmatic adds 0 blocking issues.
