---
id: QT-001
title: CLI Command Integration
type: qualification-test
status: implemented
rationale: Qualification tests verifying CLI command dispatch, create, update, and
  status operations
suspect: false
links:
- target: REQ-001
  role: verified_by
- target: STORY-039
  role: verified_by
created: '2026-04-21'
fingerprint: sha256:b19df1c1eec2
version: 2
---

# CLI Command Integration

## Test Reference

`tests/test_cli_commands.py`

## Test Cases

### TC-001: Artifact Creation
- **Input**: `specflow create --type requirement --title "Test REQ"`
- **Expected**: New REQ artifact created with sequential ID, frontmatter valid, index updated
- **Verification**: File exists in `_specflow/specs/requirements/`, `_index.yaml` updated

### TC-002: Artifact Update
- **Input**: `specflow update REQ-001 --status approved`
- **Expected**: Frontmatter status field updated, fingerprint recomputed
- **Verification**: File contains `status: approved`, fingerprint changed

### TC-003: Status Dashboard
- **Input**: `specflow status`
- **Expected**: Dashboard shows artifact counts by type, status distribution, link health
- **Verification**: Output contains counts for REQ, ARCH, STORY, etc.

### TC-004: Public API Access
- **Input**: Import `lib/ci.py` functions `_SYSTEM_PROMPT`, `_parse_batch_response`
- **Expected**: Functions accessible as public API
- **Verification**: Import succeeds, functions callable

## Acceptance Criteria

1. All test cases in `tests/test_cli_commands.py` pass
2. Tests run deterministically with no network or LLM calls
3. Tests use `tmp_path` fixtures for isolation
