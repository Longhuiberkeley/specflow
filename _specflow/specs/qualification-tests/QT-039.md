---
id: QT-039
title: 'Qualification Test for REQ-DEFERRED-5cea: Deferred lifecycle capabilities'
type: qualification-test
status: verified
rationale: "Backfilled qualification test for REQ-DEFERRED-5cea (Deferred lifecycle\
  \ capabilities: rewind accounting, RTM, RBAC check, supersession, quality gates,\
  \ multi-host). The six accounting-based command surfaces \u2014 phase-set history\
  \ record, rtm bidirectional walk, rbac CODEOWNERS resolution, supersession pairing,\
  \ AC/NFR lint parsers, multi-host detection \u2014 are exercised by tests/test_phase_set.py,\
  \ tests/test_rtm.py, tests/test_rbac_check.py, tests/test_supersession.py, tests/test_lint_quality_gates.py,\
  \ and tests/test_multi_platform.py. The aggregated wave covers all six ACs of the\
  \ REQ."
tags:
- backfilled
- verification-contract
- REQ-DEFERRED-5cea
- lifecycle
- v1.12
- accounting-not-policing
suspect: false
links:
- target: REQ-DEFERRED-5cea
  role: verified_by
- target: STORY-PHASESET-d470
  role: verified_by
- target: STORY-RTMCOMMA-1d6f
  role: verified_by
- target: STORY-RBACCHEC-e356
  role: verified_by
- target: STORY-SUPERSES-c408
  role: verified_by
- target: STORY-ARTIFACT-a39a
  role: verified_by
- target: STORY-MULTIHOS-63a5
  role: verified_by
created: '2026-08-04'
fingerprint: sha256:84008136ebfb
verify_command: uv run pytest tests/test_phase_set.py tests/test_rtm.py tests/test_rbac_check.py
  tests/test_supersession.py tests/test_lint_quality_gates.py tests/test_multi_platform.py
  tests/test_rbac.py -q
modified: '2026-08-04'
verify_run_exit_code: 0
verify_run_out_hash: sha256:ceab25a48299
verify_run_at: '2026-08-04T11:49:42Z'
verify_run_git_ref: 4d30162d8ab65c72ee7df75067253d4768e9b773
verify_run_command_hash: sha256:8a180e7bb53a
---

# Qualification Test for REQ-DEFERRED-5cea: Deferred lifecycle capabilities

Backfilled qualification test for REQ-DEFERRED-5cea (Deferred lifecycle capabilities: rewind accounting, RTM, RBAC check, supersession, quality gates, multi-host).

## Test Reference

The REQ's six ACs each have a dedicated test file that closes against one or more of the underlying stories (STORY-PHASESET-d470, STORY-RTMCOMMA-1d6f, STORY-RBACCHEC-e356, STORY-SUPERSES-c408, STORY-ARTIFACT-a39a, STORY-MULTIHOS-63a5):

- tests/test_phase_set.py — specflow phase-set records forward/rewind transitions in state history without gating; execution state clears when leaving executing.
- tests/test_rtm.py — specflow rtm prints a REQ→ARCH→STORY→tests matrix with --gaps filter and orphan-test footer.
- tests/test_rbac_check.py — specflow rbac check resolves the current git author's roles and can verify a status-transition authorization; single-user projects get a clear not-active message with exit 0.
- tests/test_supersession.py — supersession status and supersedes role for REQ/ARCH/DDD schemas.
- tests/test_lint_quality_gates.py — empty-AC error and NFR threshold warning.
- tests/test_multi_platform.py — multi-host platform detection warning and refresh --all-platforms.
- tests/test_rbac.py — peer RBAC tests (the original REQ-002 RBAC engine the rbac check builds on).

## Scope

REQ-DEFERRED-5cea closes the six deferred gaps from the 2026-07 UX analysis: reverse-lifecycle phase accounting, a bidirectional requirements-traceability matrix, a user-facing RBAC check, spec supersession for REQ/ARCH/DDD, acceptance-criteria/NFR quality gates, and multi-host skill-install awareness. All six ship in v1.12.0 as accounting/recording surfaces — none becomes a gate.

## Acceptance Criteria

1. uv run pytest tests/test_phase_set.py tests/test_rtm.py tests/test_rbac_check.py tests/test_supersession.py tests/test_lint_quality_gates.py tests/test_multi_platform.py tests/test_rbac.py -q exits 0 (recorded via specflow verify).
2. Each of the six commands surfaces gaps as warnings, not errors.
3. specflow artifact-lint --method programmatic adds 0 blocking issues.
