type: best-practice
category: spec
prefix: BP
id_format: "BP-\\d{3}$"
required_fields: [id, title, type, status, created]
optional_fields: [priority, domain, phase, tags, suspect, fingerprint, links, modified, thinking_techniques]
allowed_status:
  draft: []
  approved: [draft]
  active: [approved]
  superseded: [active]
allowed_link_roles: [derives_from, supersedes, applies_to, guided_by, complies_with]
directory: _specflow/specs/best-practices/
